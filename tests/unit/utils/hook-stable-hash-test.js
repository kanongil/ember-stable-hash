import hookStableHash from 'dummy/utils/hook-stable-hash';
import { module, test } from 'qunit';

module('Unit | Utility | hook stable hash', function() {
  // Replace this with your real tests.
  test('it works', function(assert) {
    let result = hookStableHash();
    assert.ok(result);
  });
});
